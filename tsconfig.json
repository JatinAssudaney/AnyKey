{
  "extends": "./.wxt/tsconfig.json",
  "compilerOptions": {
    "allowImportingTsExtensions": true,
    "jsx": "react-jsx",
    "types": ["node"]
  },
  // e2e/ has its own tsconfig: its code runs in Node and inside the extension's service worker.
  "exclude": ["node_modules", ".output", "e2e"]
}
