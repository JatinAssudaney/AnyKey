/**
 * The manifest's name and description, which the browser and the Chrome Web Store show. The address bar labels
 * AnyKey's own pages (the settings page) with the name, so it stays short; the description says what AnyKey does.
 */
export const APP_NAME = 'AnyKey';
export const APP_DESCRIPTION =
  'Make any website keyboard-navigable: bind keys to any element, link hints, scroll keys and site presets.';
